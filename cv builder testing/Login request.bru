meta {
  name: Login request
  type: http
  seq: 2
}

post {
  url: http://localhost:5000/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe@example.com",
    "password": "Password123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
